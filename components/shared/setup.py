"""Shared Python code for Quality-time backend components."""

from setuptools import setup

setup(
    name='shared', version='0.5.1', description='Shared Python code for Quality-time backend components',
    author='Frank Niessink', author_email='frank.niessink@ictu.nl', packages=['shared'], zip_safe=True)
