version: '3'
services:
  frontend:
    image: ictu/quality-time_frontend:v0.5.1
    depends_on:
    - server
    ports:
    - "5000:5000"
  collector:
    image: ictu/quality-time_collector:v0.5.1
    env_file:
    - ${ENV:-test}.env
    depends_on:
    - server
  server:
    image: ictu/quality-time_server:v0.5.1
    ports:
    - "5001:5001"
    env_file:
    - ${ENV:-test}.env
    depends_on:
    - database
  database:
    image: mongo
    restart: always
    env_file:
    - ${ENV:-test}.env
    ports:
    - "27017:27017"
    volumes:
    - "dbdata:/data/db"
volumes:
  dbdata:
