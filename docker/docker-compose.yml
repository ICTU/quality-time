# Base docker composition. For development, this composition is augmented in docker-compose.override.yml.

version: '3'
services:
  www:
    image: ictu/caddy:1.0.3-no-stats
    ports:
      - "80:80"
    environment:
      CONFIG: |
        :80 {
          tls off
          errors stdout
          gzip {
            not /api/v1/nr_measurements
          }
          header / {
            -Server
            Strict-Transport-Security "max-age=31536000;"
            X-XSS-Protection "1; mode=block"
            X-Content-Type-Options "nosniff"
            X-Frame-Options "DENY"
            Pragma "no-cache"
            Cache-Control "no-store"
          }
          proxy /api/v1/nr_measurements http://server:${SERVER_PORT:-5001} {
            timeout 0
          }
          proxy /api http://server:${SERVER_PORT:-5001}
          proxy / http://frontend:${FRONTEND_PORT:-5000}
        }
  frontend:
    image: ictu/quality-time_frontend:v0.17.0
    env_file:
      - ${ENV:-dev}.env
    depends_on:
    - server
  collector:
    image: ictu/quality-time_collector:v0.17.0
    env_file:
    - ${ENV:-dev}.env
    depends_on:
    - server
  server:
    image: ictu/quality-time_server:v0.17.0
    env_file:
      - ${ENV:-dev}.env
    depends_on:
    - database
  database:
    image: mongo
    restart: always
    env_file:
    - ${ENV:-dev}.env
    volumes:
    - "dbdata:/data/db"
volumes:
  dbdata:
