[project]
name = "application_tests"
version = "5.0.1"
dependencies = [
    "axe-selenium-python==2.1.6",
    "requests==2.31.0",
    "selenium==4.10.0"
]

[project.optional-dependencies]
dev = [
    "pip==23.2",
    "pip-tools==7.0.0"  # To add hashes to requirements
]
