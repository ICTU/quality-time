[project]
name = "application_tests"
version = "4.10.0"
dependencies = [
    "requests==2.30.0",
    "selenium==4.9.0"
]

[project.optional-dependencies]
dev = [
    "pip==23.1.1",
    "pip-tools==6.13.0"  # To add hashes to requirements
]
