[project]
name = "release"
version = "5.1.0"
dependencies = [
    "black==23.7.0",  # The pre-commit hook (../githooks/pre-commit) calls black
    "bump2version==1.0.1",
    "gitpython==3.1.35",
]

[project.optional-dependencies]
dev = [
    "pip==23.2.1",
    "pip-tools==7.3.0"  # To add hashes to requirements
]
