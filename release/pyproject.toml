[project]
name = "release"
version = "5.10.0"
dependencies = [
    "bump2version==1.0.1",
    "gitpython==3.1.43",
]

[project.optional-dependencies]
dev = [
    "pip==24.0",
    "pip-tools==7.4.1"  # To add hashes to requirements
]
