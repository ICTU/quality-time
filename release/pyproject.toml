[project]
name = "release"
version = "5.8.0-rc.2"
dependencies = [
    "bump2version==1.0.1",
    "gitpython==3.1.42",
]

[project.optional-dependencies]
dev = [
    "pip==24.0",
    "pip-tools==7.3.0"  # To add hashes to requirements
]
