[project]
name = "release"
version = "5.4.0"
dependencies = [
    "bump2version==1.0.1",
    "gitpython==3.1.40",
]

[project.optional-dependencies]
dev = [
    "pip==23.3.1",
    "pip-tools==7.3.0"  # To add hashes to requirements
]
