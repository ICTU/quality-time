[project]
name = "release"
version = "5.0.0-rc.2"
dependencies = [
    "black==23.3.0",  # The pre-commit hook (../githooks/pre-commit) calls black
    "bump2version==1.0.1",
    "gitpython==3.1.31",
]

[project.optional-dependencies]
dev = [
    "pip==23.1.2",
    "pip-tools==6.13.0"  # To add hashes to requirements
]
