[project]
name = "release"
version = "4.9.0"
dependencies = [
    "bump2version==1.0.1",
    "gitpython==3.1.31",
]

[project.optional-dependencies]
dev = [
    "pip==23.0.1",
    "pip-tools==6.13.0"  # To add hashes to requirements
]
