[project]
name = "release"
version = "5.3.1"
dependencies = [
    "black==23.10.1",  # The pre-commit hook (../githooks/pre-commit) calls black
    "bump2version==1.0.1",
    "gitpython==3.1.40",
]

[project.optional-dependencies]
dev = [
    "pip==23.3.1",
    "pip-tools==7.3.0"  # To add hashes to requirements
]
